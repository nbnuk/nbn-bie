rootProject.name='nbn-bie'

boolean inplace = true

if (inplace) {
    include ':bie-plugin'
    project(':bie-plugin').projectDir = new File(settingsDir,'../bie-plugin')
    include ':ala-charts-plugin'
    project(':ala-charts-plugin').projectDir = new File(settingsDir,'../ala-charts-plugin')
    //ala-charts: need to checkout master (uses grails 3.2.11) instead of e.g. 1.3.x used by nbn-hub
}